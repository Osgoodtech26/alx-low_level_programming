#include <stdio.h>
/**
 * main - A writing a c program that prints a line using a print f function
 *  Return:0(success)
 */
int main(void)
{ 
printf("with proper grammar, but the outcome is a piece of art,\n")
return(0);
}
